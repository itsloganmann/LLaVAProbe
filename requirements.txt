# =============================================================================
# LLaVAProbe Requirements - GPU Optimized (Google Cloud & Google Colab)
# =============================================================================
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# GOOGLE COLAB SETUP (Recommended for Easy Testing)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 1. Enable GPU: Runtime > Change runtime type > Hardware accelerator > GPU (T4)
# 2. Run in Colab cell:
#    !git clone https://github.com/itsloganmann/LLaVAProbe.git
#    %cd LLaVAProbe
#    !git checkout "Idea-#3----Attention-Evolution-Tracking"
#    !pip install -q -r requirements.txt
#    
#    # Copy custom model files
#    import transformers, os, shutil
#    trans_path = os.path.dirname(transformers.__file__)
#    shutil.copy("modeling_llava.py", f"{trans_path}/models/llava/modeling_llava.py")
#    shutil.copy("modeling_llama.py", f"{trans_path}/models/llama/modeling_llama.py")
#
# 3. LLaVA will auto-download from HuggingFace on first run (cached in Colab session)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# GOOGLE CLOUD VM SETUP
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# - Instance: n1-standard-8 or higher
# - GPU: NVIDIA T4, V100, or A100
# - OS: Ubuntu 20.04 LTS or later with CUDA 11.8+
# - Disk: 100GB+ boot disk recommended
#
# Quick Install (with CUDA support):
#   pip install torch==2.1.2 torchvision==0.16.2 --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# =============================================================================

# Core ML dependencies (CUDA 11.8 compatible)
torch==2.5.1
torchvision==0.16.2
transformers==4.37.1
accelerate==0.26.1
huggingface-hub==0.20.3

# Image processing
Pillow==10.2.0
opencv-python==4.10.0.84

# Data processing and analysis
numpy==1.26.4
pandas>=2.0.0
scikit-learn==1.4.2

# Clustering and dimensionality reduction
hdbscan==0.8.33
umap-learn==0.5.6

# Quantization support (REQUIRED for memory-efficient inference on Google Cloud)
# Enables 4-bit/8-bit model loading - crucial for GPU memory optimization
bitsandbytes==0.42.0

# HTTP requests
requests==2.32.3

# Jupyter notebook support (optional - comment out if running headless on GCP)
jupyter>=1.0.0
ipykernel==6.29.4
ipython==8.12.3

# Visualization
matplotlib==3.7.4

# Visualization and utilities
bertviz==1.4.0
pypdf==6.1.3

# Development and testing (optional)
pytest>=7.4.0
pytest-cov>=4.1.0

